import { Modal as StrapModal, ModalBody, ModalFooter, ModalHeader } from 'reactstrap';

const Modal = StrapModal;

Modal.Body = ModalBody;
Modal.Header = ModalHeader;
Modal.Footer = ModalFooter;
export default Modal;